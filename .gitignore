node_modules
.next
out
.env
.env*.local
npm-debug.log*

# Claude Code — local-only overrides (keep shared .claude/settings.json + rules committed)
CLAUDE.local.md
.claude/settings.local.json

# Never commit (legacy helper; may have been used with tokens historically)
push_to_github.bat

# Backup credentials (DB connection string + R2 keys + age recipient) — see deploy/BACKUP.md
.backup.env
*.key
