# คู่มือระบบสะสมแต้ม (Loyalty) — Minimal Maerim

สำหรับพนักงาน / ผู้จัดการ / Admin  
ระบบอยู่ที่เมนู **สะสมแต้ม** → `https://minimalcnx.ddserviceth.com/loyalty`

> เอกสารนี้เป็นคู่มือใช้งานร้าน  
> รายละเอียดเทคนิค/ความปลอดภัย/SQL ดูที่ [`CLAUDE.md`](./CLAUDE.md)

---

## 1. ภาพรวมระบบปัจจุบัน

ระบบแต้มทำงาน **ในเว็บหลังบ้านที่พนักงานล็อกอินอยู่แล้ว** ไม่ต้องให้ลูกค้าติดตั้งแอป

| ใคร | ทำอะไรได้ |
|---|---|
| พนักงาน (staff) | ค้นหา/สมัครลูกค้า, แจกแต้ม, แลกรางวัล, ดูประวัติธุรกรรม (+ เมนูร้านอื่นตามสิทธิ์) |
| **Loyalty only** (`loyalty_staff`) | เฉพาะสะสมแต้ม — เหมาะสาขาพาร์ทเนอร์ / เคาน์เตอร์แต้มอย่างเดียว |
| Manager / Co-Admin / Admin | ทั้งหมดของพนักงาน + แดชบอร์ด CDP + **ยกเลิกธุรกรรม** |
| Admin / Co-Admin | ตั้งค่าสาขา + ผูกพนักงานกับสาขา + ของรางวัล (`/admin/loyalty`) — **ต้องผูกก่อนพนักงานถึงจะแจก/แลกได้** |

### มือถือ (แนะนำ)

1. เปิด Safari / Chrome ไปที่ `https://minimalcnx.ddserviceth.com/login`
2. เข้าสู่ระบบด้วยบัญชีพนักงาน (สาขาพาร์ทเนอร์ใช้ role **Loyalty only**)
3. เมนูแชร์ / ⋮ → **Add to Home Screen** / เพิ่มไปหน้าจอโฮม  
   แอปจะเปิดตรงหน้าสะสมแต้มแบบเต็มจอ (PWA)
4. ตอนสมัครสมาชิกใหม่ ต้องติ๊ก **ความยินยอมเก็บข้อมูลส่วนบุคคล (PDPA)** ทุกครั้ง

**อัตราแต้มแนะนำ:** ทุก 50 บาท = 1 แต้ม (ระบบคำนวณให้เมื่อพิมพ์ยอดซื้อ — พนักงานแก้จำนวนแต้มได้ก่อนกดบันทึก แต่ไม่เกิน 100 แต้ม/ครั้ง)

**รางวัลปัจจุบัน**

| รางวัล | แต้มที่ใช้ |
|---|---|
| กาแฟฟรี 1 แก้ว (ร้อน/เย็น) | 10 |
| ขนมหน้าร้านฟรี 1 ชิ้น | 15 |
| ส่วนลด 50 บาท | 20 |

**สาขา:** ระบบสะสมแต้มรองรับหลายสาขา (เช่น แม่ริม / ตัวเมือง)  
**หมายเหตุ:** ยอดขาย / รายจ่าย / OPEX / รายงานบัญชี ยังเป็น “ร้านเดียว” ไม่ได้แยกสาขา

---

## 2. ก่อนเปิดใช้จริง (Admin ทำครั้งเดียว)

1. ให้แน่ใจว่ารัน SQL บน Supabase แล้ว  
   - `sql/add_loyalty_system.sql`  
   - `sql/harden_loyalty_rls.sql`
2. Deploy โค้ดล่าสุดบน VPS: `cd ~/apps/minimalcnx && bash deploy.sh`
3. เข้า **Admin → สาขา / พนักงานสะสมแต้ม** (`/admin/loyalty`)
   - ตรวจสาขา (มี seed: `MAIN` แม่ริม, `CNX01` ตัวเมือง)
   - **ผูกพนักงานแต่ละคนเข้าสาขา** + ใส่รหัสพนักงาน (เช่น `EMP01`)  
     → **บังคับ** — ถ้าไม่ผูก พนักงานแจก/แลกแต้มไม่ได้
4. ทดลองกับเบอร์ทดสอบ: สมัคร → แจกแต้ม (ใส่เลขใบเสร็จ) → แลกรางวัล → ดูประวัติ/แดชบอร์ด

---

## 3. ขั้นตอนพนักงานตอนขาย

### 3.1 ค้นหาลูกค้า
1. เปิดเมนู **สะสมแต้ม**
2. พิมพ์ **เบอร์โทร** (ตัวเลขอย่างเดียว — เร็วสุด) หรือ LINE User ID แล้วกดค้นหา  
   หรือกดปุ่ม **เบอร์ล่าสุด** ใต้ช่องค้นหา (จำในเครื่องนี้)
3. ถ้าเจอ → ไปขั้นแจกแต้ม  
   ถ้าไม่เจอ → กรอกชื่อแล้วกดสมัครสมาชิก

### 3.2 แจกแต้มหลังชำระเงิน
1. ตรวจ **สาขา** (ระบบเลือกสาขาตั้งต้นจากที่ Admin ผูกไว้)
2. พิมพ์ **ยอดซื้อ (บาท)**
3. ใส่ **เลขที่ใบเสร็จ** (บังคับ)
4. กด **แจกตามยอด** (ปุ่มเร็ว — ใช้สูตร 50฿=1 แต้ม)  
   หรือแก้จำนวนแต้มแล้วกดบันทึกสะสม

**ระบบกันทุจริตอัตโนมัติ**
- **ใบเสร็จ 1 ใบ = แจกแต้มได้ครั้งเดียว** (นับแยกตามสาขา) — ถ้าใส่เลขที่เคยใช้แล้ว ระบบจะปฏิเสธ พร้อมบอกว่าใบนั้นถูกใช้เมื่อไหร่และให้ลูกค้าคนไหน  
  ถ้าแจกผิดจริงๆ ให้ **ผู้จัดการยกเลิกรายการเดิม** ที่หน้าประวัติก่อน แล้วค่อยแจกใหม่ (ระบบจะเก็บทั้งรายการเดิมและรายการยกเลิกไว้ให้ตรวจสอบได้)
- แจกเกิน 100 แต้ม/ครั้ง → ปฏิเสธ + บันทึกแจ้งเตือน
- แจกให้ลูกค้ารายเดิมถี่ผิดปกติ (≥5 ครั้งใน 10 นาที โดยพนักงานคนเดียวกัน) → ปฏิเสธ + บันทึกแจ้งเตือน
- ไม่มีสาขาที่ผูก / ไม่ใส่ใบเสร็จ → บันทึกไม่ได้

### 3.3 แลกรางวัล
1. ค้นหาลูกค้า → ดูแต้มคงเหลือ
2. กดรางวัลที่ต้องการ → ยืนยัน
3. ระบบหักแต้มทันที และบันทึกว่า **แลกที่สาขาไหน / พนักงานคนไหน**

### 3.4 ประวัติธุรกรรม (ตรวจย้อนหลัง)
เมนู **ประวัติธุรกรรม** (`/loyalty/history`) — กรองได้ตามสาขา / พนักงาน / ประเภท / เบอร์ / วันที่  
ตอบคำถาม: สาขาไหนให้แต้ม · ใครให้ · แลกที่ไหน

### 3.5 เคล็ดลับหน้าเคาน์เตอร์
- ถามเบอร์ตอนคิดเงิน → พิมพ์ยอด → ใส่เลขใบเสร็จ → กด **แจกตามยอด**
- ใช้ปุ่มเบอร์ล่าสุดเมื่อลูกค้าประจำกลับมา
- แจ้งลูกค้าด้วยวาจาว่าเหลือกี่แต้ม / อีกกี่แต้มได้กาแฟฟรี

---

## 4. ผู้จัดการ: ประวัติ + แดชบอร์ด CDP

### ประวัติธุรกรรม
`/loyalty/history` — ทุกคนที่ล็อกอินดูได้  
**ยกเลิกธุรกรรม** (สร้างรายการย้อนกลับ ไม่ลบของเดิม) ได้เฉพาะ manager / co-admin / admin พร้อมใส่เหตุผล

### แดชบอร์ดวิเคราะห์ (CDP)
`/loyalty/analytics` — admin / co-admin / manager

ดูได้โดยสังเขป:
- แต้มที่แจก / แลก รวม + การแจ้งเตือนทุจริต
- รายงานสาขา (ทั้งหมด / เดือนนี้)
- รายการแลกที่อาจข้ามสาขา
- กลุ่มลูกค้า RFM
- ผลงานพนักงาน + audit

---

## 5. Admin: จัดการสาขาและพนักงาน

ที่ `/admin/loyalty` (หรือลิงก์จากหน้า Admin / หน้าสะสมแต้ม)

- **เพิ่ม/ปิดสาขา** — รหัสสาขาต้องไม่ซ้ำ (เช่น `MAIN`, `CNX01`)
- **ผูกพนักงาน ↔ สาขา** — เลือก user ในระบบ + สาขา + รหัสพนักงาน  
- ลบการผูกได้เมื่อพนักงานย้าย/ลาออก (ไม่ลบประวัติแต้มลูกค้า)

---

## 6. เปรียบเทียบ: ระบบปัจจุบัน vs แต้มใน LINE (กดแจ้ง → ลูกค้าสแกน QR)

สองแนวนี้คนละ “ประสบการณ์หน้าเคาน์เตอร์” — ไม่ใช่แค่คนละหน้าจอ

### 6.1 ระบบปัจจุบัน (พนักงานค้นเบอร์แล้วกดแจกแต้ม)

```
ลูกค้าบอกเบอร์ → พนักงานค้นในเว็บ → กดแจก/แลก → บอกยอดแต้มปากเปล่า
```

| ข้อดี | ข้อควรระวัง |
|---|---|
| ไม่ต้องมี LINE OA / ไม่พึ่งแอปลูกค้า | ลูกค้าไม่มี push / ไม่เห็นแต้มเองในมือถือ |
| ใช้เบอร์โทร — คนไม่มี LINE ก็สะสมได้ | พนักงานพิมพ์เบอร์ผิดได้ (ตรวจชื่อก่อนกด) |
| ควบคุมและกันทุจริตฝั่งร้านได้ชัด (จำกัดแต้ม, rate-limit, audit, สาขา) | ช่วงร้านแน่น อาจช้ากว่าการยื่น QR ให้สแกน |
| รวมกับรายงาน/CDP ในระบบเดิมได้ทันที | ประสบการณ์ลูกค้ารู้สึก “ร้านจัดการให้” มากกว่า “สมาชิกดิจิทัล” |
| ค่าใช้จ่ายและงานดูแลน้อยกว่า | — |

### 6.2 แบบ LINE: พนักงานกดแจ้ง → ลูกค้าสแกน QR

```
พนักงานเปิดบิล/กด “แจ้งสะสม” → โชว์ QR → ลูกค้าสแกนใน LINE → ได้แต้ม + เห็นในแชท
```

| ข้อดี | ข้อควรระวัง |
|---|---|
| ลูกค้าเห็นแต้ม/ประวัติใน LINE เอง ลดคำถามซ้ำ | ต้องมี LINE Official Account + พัฒนา LIFF/Webhook + ดูแลต่อเนื่อง |
| ลดการพิมพ์เบอร์ตอนคิวแน่น (ถ้า flow ดี) | คนไม่มี LINE / ไม่ยอมแอด OA เข้าระบบยาก |
| ยิงข้อความโปรโมชัน/เตือนแต้มใกล้หมดได้ | QR ที่ออกแบบไม่ดีถูกแคป/แชร์/สแกนซ้ำได้ — ต้องออกแบบ one-time / หมดอายุ |
| ภาพลักษณ์ทันสมัย ผูกแบรนด์ในแชทลูกค้า | พนักงานยังต้อง “กดเปิดรอบสะสม” ถูกบิล — ผิดบิลแล้วยากไล่ |
| — | ต้นทุน/เวลาพัฒนาสูงกว่า และข้อมูลแต้มแยกจากระบบบัญชีร้านถ้าไม่เชื่อมให้ดี |

### 6.3 อะไร “ดีกว่า”?

**สำหรับร้านตอนนี้ — ระบบพนักงานในเว็บ (แบบปัจจุบัน) เหมาะกว่าเป็นหลัก**

เหตุผลสั้นๆ:
1. **เปิดใช้ได้ทันที** โดยไม่รอ LINE OA / QR / LIFF  
2. **คุมทุจริตและแยกสาขาได้ในระบบเดียวกับที่ร้านใช้อยู่แล้ว**  
3. **ลูกค้าไม่มี LINE ก็สะสมได้** (สำคัญสำหรับลูกค้า walk-in)  
4. แบบ LINE/QR **ดีกว่าเรื่องประสบการณ์ลูกค้าและการตลาด** แต่เป็นชั้นเสริม ไม่ใช่ตัวแทนระบบควบคุมแต้มของร้าน

**แนวทางที่แนะนำระยะกลาง**
- ใช้ระบบปัจจุบันเป็นแหล่งความจริงของแต้ม (source of truth)
- ถ้าจะทำ LINE ภายหลัง: ให้ LINE เป็นช่องทาง “ดูแต้ม / รับแจ้งเตือน / สแกนรับแต้ม” ที่ยิงเข้าฐานข้อมูลชุดเดียวกัน  
  ไม่สร้างระบบแต้มคนละก้อนใน LINE อย่างเดียว — จะยอดไม่ตรงและไล่ทุจริตยาก

| สถานการณ์ | เลือก |
|---|---|
| อยากเริ่มสะสมแต้มสัปดาห์นี้, ทีมเล็ก | **ระบบปัจจุบัน** |
| คิวแน่นมากทุกวัน, ลูกค้าติด LINE อยู่แล้ว, มีงบพัฒนา | พิจารณา **เพิ่ม QR บน LINE** ทีหลัง |
| กลัวพนักงานกดแต้มมั่ว | **ระบบปัจจุบัน** (+ ผูกสาขา + ดู audit ใน CDP) |
| อยากยิงโปร/เตือนกลับมาใช้บริการ | **LINE เสริม** (เมื่อพร้อม) |

---

## 7. คำถามที่พบบ่อย

**ลูกค้าเปลี่ยนเบอร์ทำอย่างไร?**  
ตอนนี้ค้นด้วยเบอร์เป็นหลัก — Admin/ผู้ดูแลต้องแก้ในฐานข้อมูลหรือลงทะเบียนเบอร์ใหม่แล้วโอนแต้มด้วยมือ (ยังไม่มีหน้าโอนเบอร์ใน UI)

**พิมพ์จำนวนแต้มผิด?**  
ให้ Manager เปิด **ประวัติธุรกรรม** → กด **ยกเลิก** บนรายการนั้น ใส่เหตุผล  
ระบบจะสร้างรายการย้อนกลับ (ไม่ลบของเดิม) เพื่อให้ตรวจสอบได้

**พนักงานคนอื่นสาขาเห็นข้อมูลลูกค้าไหม?**  
สมาชิกเป็นคลังรวมทั้งร้าน — ใช้เบอร์เดียวกันสะสมข้ามสาขาได้ ธุรกรรมจะติดสาขาที่แจก/แลก

**Staff เข้าแดชบอร์ด CDP ได้ไหม?**  
ไม่ได้ — ระบบเด้งกลับหน้าสะสมแต้ม

---

## 8. สรุป checklist ประจำวัน

- [ ] ล็อกอินด้วยบัญชีของตัวเอง (ไม่แชร์รหัส)
- [ ] ตรวจว่าสาขาที่เลือกถูกต้อง
- [ ] ทุกบิลที่มีสมาชิก → ค้นเบอร์ → แจกแต้มก่อนลูกค้าเดินออก
- [ ] แลกรางวัลเฉพาะเมื่อยืนยันตัวตน/เบอร์ชัด
- [ ] ถ้ามีข้อความปฏิเสธทุจริต → หยุดแล้วเรียกผู้จัดการ
